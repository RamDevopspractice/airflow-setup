executor: KubernetesExecutor

dags:
  gitSync:
    enabled: true
    repo: https://github.com/RamDevopspractice/airflow-setup.git
    branch: main
    subPath: "dags"
    period: 60s
    credentialsSecret: git-credentials
    image:
        repository: k8s.gcr.io/git-sync/git-sync   # ✅ FIX
        tag: v4.4.2